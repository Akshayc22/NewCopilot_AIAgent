# Angular UI Generation Prompts

## Component Creation
```
Create a new Angular component for [feature name] with:
- TypeScript class with proper lifecycle hooks
- Template with modern HTML5 semantic elements
- SCSS styling with CSS Grid/Flexbox for responsive design
- Input/Output properties for component communication
- Change detection optimization using OnPush strategy
- Reactive forms integration where applicable
- Accessibility attributes and ARIA labels
- Angular Material or custom design system components

Component structure:
- OnInit for initialization logic
- OnDestroy for cleanup and unsubscription
- ViewChild/ContentChild for DOM access
- HostListener for event handling
- Proper error handling with try-catch blocks
- Unit test file with comprehensive test cases
```

## Reactive Forms
```
Generate Angular reactive forms for [form purpose] with:
- FormBuilder service for form construction
- Custom validators for business logic validation
- Async validators for server-side validation
- Dynamic form controls based on user input
- Form array handling for dynamic lists
- Cross-field validation between form controls
- Proper error message display and styling
- Form state management (pristine, dirty, valid, invalid)

Form features:
- Auto-save functionality with debounce
- Form reset and default value handling
- Conditional field display based on form state
- File upload with progress tracking
- Multi-step form wizard with navigation
- Form accessibility with proper labeling
```

## Data Display Components
```
Create Angular data display components for [data type] with:
- Angular Material table or custom data grid
- Sorting, filtering, and pagination capabilities
- Virtual scrolling for large datasets
- Export functionality (CSV, PDF, Excel)
- Row selection and bulk operations
- Responsive design for mobile devices
- Loading states and error handling

Data features:
- Search functionality with highlighting
- Column customization and reordering
- Inline editing capabilities
- Row expansion for detailed views
- Drag and drop for row reordering
- Context menus for row actions
```

## Navigation and Routing
```
Implement Angular navigation for [app section] with:
- Hierarchical routing with nested routes
- Route guards for authentication and authorization
- Lazy loading for feature modules
- Route resolvers for data pre-loading
- Query parameters and route parameters handling
- Breadcrumb navigation component
- Route animations and transitions

Navigation features:
- Side navigation with collapsible menu
- Tab navigation with route synchronization
- Stepper navigation for multi-step processes
- Back button handling and browser history
- Deep linking support for bookmarkable URLs
- Route-based state management
```

## Responsive Design
```
Create responsive Angular layouts for [layout type] using:
- CSS Grid and Flexbox for flexible layouts
- Angular Flex Layout or CSS media queries
- Breakpoint observer service for responsive behavior
- Mobile-first design approach
- Touch-friendly interface elements
- Progressive web app (PWA) considerations
- Performance optimization for mobile devices

Responsive features:
- Adaptive navigation (drawer on mobile, tabs on desktop)
- Responsive data tables (stack on mobile)
- Image optimization and lazy loading
- Touch gestures for mobile interactions
- Viewport meta tag configuration
- Device-specific styling and behavior
```